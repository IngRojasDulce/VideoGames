const { Gender, Videogame } = require("../db");
// const { Op } = require("sequelize");
//const axios = require("axios");

const getallVideogames = (name)=>{
if(name){
return( "hay usuario")
}


return ("aqui van todos")
}
module.export={
    getallVideogames
}
